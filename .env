# .env (CORREGIDO PARA MONGODB Y TOKEN_SECRET)

# URL de conexión a tu base de datos MongoDB.
# Descomenta esta línea y usa tu propia URL de conexión de MongoDB Atlas o local.
MONGODB_URI=mongodb+srv://FerCalc:Mundial2014@fercalcpro.5irlwpw.mongodb.net/fercalc-db?retryWrites=true&w=majority&appName=FerCalcPro

# Secreto para firmar los JSON Web Tokens. Debe ser el mismo que usas en jwt.js y validateToken.js
TOKEN_SECRET="ESTE_ES_UN_SECRETO_MUY_LARGO_Y_DIFICIL_DE_ADIVINAR"

# Comentamos o eliminamos la variable de PostgreSQL para evitar confusiones
# DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE"